/* common.h
 * Contains common definitions that are
 * required throughout the program.
 */

#ifndef COMMON_H
#define COMMON_H

/* Assertions to catch bugs, not errors. */
#include <assert.h>
#include <stddef.h>

#define TRUE (1)
#define FALSE (!TRUE)

#endif
